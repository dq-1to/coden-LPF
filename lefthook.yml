pre-commit:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
    lint:
      run: npm run lint

pre-push:
  parallel: true
  commands:
    test:
      run: npm run test
    build:
      run: npm run build
