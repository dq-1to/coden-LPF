# 2026-04-05

## v3 M1 基盤構築（T1〜T8）

**ブランチ**: v3/m1-foundation
**PR**: #199（→ dev マージ済み）
**目的**: v3.0 モバイル対応の基盤を構築（CodeMirror 6、モバイルフック、PWA、タッチ最適化）

### 実施内容
- [x] T1: CodeMirror 6 パッケージインストール + Vite manualChunks 設定
- [x] T2: CodeEditor ラッパーコンポーネント作成（forwardRef + insertAtCursor）
- [x] T3: useIsMobile フック作成（matchMedia 768px）
- [x] T4: useKeyboardVisible フック作成（VisualViewport API）
- [x] T5: touch-action: manipulation + safe-area-inset を globals.css に追加
- [x] T6: manifest.json + PWA meta タグ + アイコン生成（sharp-cli）
- [x] T7: sw.js（Network-first）+ main.tsx に SW 登録
- [x] T8: CodeEditor / useIsMobile / useKeyboardVisible のテスト作成

### エージェント構成
- 構成: Opus 直接 + Sonnet×2 並列サブエージェント（worktree isolation）
- 切り分け:
  - T1, T5, T6, T7: Opus 直接（順次実行）
  - T2: Opus 直接（Context7 で @uiw/react-codemirror API 参照）
  - T3: Sonnet サブエージェント（worktree）
  - T4: Sonnet サブエージェント（worktree）— T3 と並列実行
  - T8: Opus 直接

### ツール使用
- プラグイン: Context7（@uiw/react-codemirror ドキュメント参照）
- スキル: なし

### 判断の記録
- T2 で Monaco Editor からの移行ではなく新規 CodeEditor コンポーネントとして作成。Monaco は M2 で撤去予定のため共存させる
- T4 の VisualViewport API は閾値 150px を採用（キーボード高さの一般的な最低値）
- sw.js は ESLint ignores に追加（Service Worker グローバル変数が no-undef に引っかかるため）

### 発見・ハマり・気づき
- worktree isolation のサブエージェントが一部メインツリーにファイルを書き込む問題が発生（T4）。手動で修正
- vi.spyOn(window, 'matchMedia') は jsdom で undefined のため失敗 → vi.stubGlobal() を使用
- vi.mock のホイスティングで外部変数参照エラー → vi.hoisted() で解決
- CodeEditor テストで複数 render による DOM 重複 → mock.calls 引数検証に切替

### エラー・障害
- TS6133: unused import beforeEach in useKeyboardVisible.test.ts → 削除で解決
- TS2532: Object possibly undefined in useIsMobile.test.ts → toHaveBeenCalledWith に簡略化
- TS2493: Tuple type of length 0 in CodeEditor.test.tsx → unknown[][] にキャスト
- ESLint no-undef: sw.js の self/caches/fetch → eslint ignores に追加

### 影響範囲
- 変更: 14ファイル（新規10、修正4）

### CI結果
- typecheck: PASS / lint: PASS / test: PASS（643件 = 629既存 + 14新規） / build: PASS

### 昇華候補メモ
- [RuleOps/tooling.md] worktree isolation サブエージェントのファイル書き込み先に注意。メインツリーに漏れる場合がある
- [MEMORY.md] vi.hoisted() パターンは CodeMirror 等の大型ライブラリモック時に必須

---

## v3 M2 エディタ移行（T9〜T14）

**ブランチ**: v3/m2-editor-migration
**PR**: #201（→ dev マージ済み）
**目的**: Monaco Editor → CodeMirror 6 完全移行（ChallengeMode / CodeDoctorPage / MiniProjectDetailPage）

### 実施内容
- [x] T9: ChallengeMode の Monaco → CodeEditor 置換
- [x] T10: CodeDoctorPage の Monaco → CodeEditor 置換
- [x] T11: MiniProjectDetailPage の Monaco → CodeEditor 置換
- [x] T12: @monaco-editor/react アンインストール + vendor-monaco チャンク削除
- [x] T13: ErrorBoundary / Suspense ラッパー除去
- [x] T14: CI 全通過 + バンドルサイズ確認（vendor-monaco 消滅）

### エージェント構成
- 構成: Opus 直接（サブエージェント不使用）
- 切り分け: 全タスク [Sonnet] レベルの定型置換のため直接実行が最も効率的と判断

### ツール使用
- プラグイン: なし
- スキル: なし

### 判断の記録
- 全6タスクが定型置換かつ依存関係が直列のため、サブエージェント並列化より直接実行を選択
- handleCodeChange の型を string | undefined → string に変更（CodeEditor は undefined を返さない）
- Spinner import は MiniProjectDetailPage でのみ Suspense fallback 専用だったため削除

### 発見・ハマり・気づき
- vi.mock のパスはテストファイルからの相対パスで解決される。`../../components/CodeEditor` は test ファイル位置から `src/features/components/CodeEditor` に解決されてしまう → `@/components/CodeEditor` エイリアスを使用

### エラー・障害
- ChallengeMode テストで vi.mock パス不一致（相対パス vs エイリアス）→ `@/components/CodeEditor` に修正

### 影響範囲
- 変更: 9ファイル、+34/-125行

### CI結果
- typecheck: PASS / lint: PASS / test: PASS（643件） / build: PASS
- バンドル: vendor-monaco 消滅、vendor-codemirror 398 kB（変更なし）

### 昇華候補メモ
- [RuleOps/implementation.md] vi.mock のパス解決: テストファイル位置からの相対パスに注意。features/ 以下のテストは `@/` エイリアスを使う方が安全

---

## v3 M3 コードツールバー（T15〜T21）

**ブランチ**: v3/m3-code-toolbar
**PR**: #202（→ dev マージ済み）
**目的**: モバイル専用コードツールバーの作成と3ページへの統合 + エディタ高さレスポンシブ化

### 実施内容
- [x] T15: CodeToolbar コンポーネント作成（記号バー11種 + キーワードサジェスト最大8個）
- [x] T16: CodeEditor に toolbarKeywords Props 追加、insertAtCursor 連携
- [x] T17: ChallengeMode にツールバー統合（expectedKeywords）
- [x] T18: CodeDoctorPage にツールバー統合（requiredKeywords）
- [x] T19: MiniProjectDetailPage にツールバー統合（全マイルストーン requiredKeywords）
- [x] T20: エディタ高さレスポンシブ化（PC固定 / モバイル min(50vh, 300px)）
- [x] T21: CodeToolbar テスト6件 + 既存テスト useIsMobile モック追加

### エージェント構成
- 構成: Opus 直接（サブエージェント不使用）
- 切り分け: T15-T16 は Opus 直接（新規設計）、T17-T20 は定型なので直接実行、T21 直接

### ツール使用
- プラグイン: なし
- スキル: なし

### 判断の記録
- onPointerDown + preventDefault を使用（onClick ではなく）: エディタにフォーカスを戻すため、ボタンタップでフォーカスが移動しない設計
- MiniProjectDetailPage の toolbarKeywords は全マイルストーンの requiredKeywords を flatMap で集約（現在のマイルストーンだけでなく全体を見せる方が学習効果が高い）
- insertAtCursor は M1 で既に実装済みだったため、T16 は Props 追加とレイアウト変更のみ

### 発見・ハマり・気づき
- useMemo が早期 return（Navigate）の後にあると react-hooks/rules-of-hooks 違反 → 早期 return 前に移動
- CodeEditor/ChallengeMode/CodeDoctorPage テストに useIsMobile モックが必要だった（CodeEditor が内部で useIsMobile を呼ぶため）

### エラー・障害
- ESLint react-hooks/rules-of-hooks: MiniProjectDetailPage の useMemo が条件付き return 後 → フック宣言を早期 return 前に移動

### 影響範囲
- 変更: 9ファイル（新規2、修正7）、+211/-28行

### CI結果
- typecheck: PASS / lint: PASS / test: PASS（649件 = 643既存 + 6新規） / build: PASS

### 昇華候補メモ
- [RuleOps/implementation.md] 新しいフックを共通コンポーネントに追加すると、そのコンポーネントをモックしていない全テストに影響する。テスト修正を忘れないこと

---

## v3 M4 コードパズル（T22〜T29）

**ブランチ**: v3/m4-code-puzzle
**PR**: #203（→ dev マージ済み）
**目的**: Test モードのモバイル専用 UI「コードパズル」作成（トークン組み立て方式）

### 実施内容
- [x] T22: useTokenGenerator フック作成（tokenize + generateDistractors + seededShuffle）
- [x] T23: Token コンポーネント作成（pool/assembled バリアント、44px タッチターゲット）
- [x] T24: TokenPool コンポーネント作成（使用済みグレーアウト、flex-wrap）
- [x] T25: AssemblyArea コンポーネント作成（空状態プレースホルダー、タップ取り消し）
- [x] T26: CodeContext コンポーネント作成（Prism.js ハイライト + ____ インジケーター）
- [x] T27: CodePuzzle コンテナ作成（全サブコンポーネント統合 + 判定接続）
- [x] T28: TestMode PC/モバイル分岐（useIsMobile + handlePuzzleSubmit）
- [x] T29: テスト作成 + CI 全通過（677件 = 649既存 + 28新規）

### エージェント構成
- 構成: Opus 直接 + Sonnet×4 並列サブエージェント
- 切り分け:
  - T22: Opus 直接（新規アルゴリズム設計）
  - T23: Sonnet サブエージェント（Token コンポーネント）
  - T24: Sonnet サブエージェント（TokenPool）— T23 と並列
  - T25: Sonnet サブエージェント（AssemblyArea）— T23 と並列
  - T26: Sonnet サブエージェント（CodeContext）— T23 と並列
  - T27: Opus 直接（統合設計）
  - T28-T29: Opus 直接（TestMode 分岐 + テスト）

### ツール使用
- プラグイン: なし
- スキル: なし

### 判断の記録
- T23-T26 は全て独立したファイル作成のため4並列サブエージェント実行。M1 の worktree 問題を回避するため worktree isolation は不使用
- Token コンポーネントは CodeToolbar と異なり onClick を使用（CodePuzzle はエディタフォーカスを維持する必要がない）
- CodePuzzle の handleRemoveToken は組み立てエリアのトークンからプールインデックスを逆引きして返却する設計

### 発見・ハマり・気づき
- expectedKeywords.join('') のトークン化: ['setCount', 'count - 1'] → 'setCountcount - 1' → tokenize → ['setCountcount', '-', '1']。keywords 間にセパレーターがないため結合される。テストでは単一キーワードの testTask を使用して回避
- TestMode が CodePuzzle を import → CodeContext が prismjs を import → prismjs/components/prism-tsx が Prism.languages.jsx の未定義でエラー。TestMode テストに prismjs モック追加が必要
- ESLint no-non-null-assertion: 配列アクセス `code[i]!` が13箇所で違反。`charAt()` メソッドに置換して解決

### エラー・障害
- TS2532: Object possibly undefined in CodePuzzle.test.tsx → `as string[]` キャストで解決
- prismjs TypeError in TestMode.test.tsx → prismjs + prism-typescript + prism-tsx をモック追加
- CodePuzzle テストで 'setCount' ボタンが見つからない → expectedKeywords 結合後のトークン化結果を考慮して testTask を修正

### 影響範囲
- 変更: 11ファイル（新規9、修正2）、+772/-44行

### CI結果
- typecheck: PASS / lint: PASS / test: PASS（677件 = 649既存 + 28新規） / build: PASS

### 昇華候補メモ
- [RuleOps/implementation.md] prismjs をインポートするコンポーネントを間接的に import するテストでは、prismjs + 言語コンポーネントのモックが必要
- [RuleOps/implementation.md] useTokenGenerator の expectedKeywords.join('') は keywords 間のセパレーターがないため、トークン境界が意図と異なる場合がある。テスト設計時に注意

---

## design-audit スキル作成 + v3 修正ロードマップ

**ブランチ**: dev（直接作業）
**目的**: Design Principles 7原則監査の Custom Skill 化 + v3roadmap01-2.md の FAIL 18件を修正ロードマップに変換

### 実施内容
- [x] `/design-audit` スキル（SKILL.md）を新規作成
- [x] CLAUDE.md Tool Triggers に `/design-audit` 行を追加
- [x] RuleOps/tooling.md の UI/UX Skills 構成表に追記
- [x] v3roadmap02.md（修正ロードマップ）を新規作成 — FAIL 18件 → 修正タスク H-A〜H-J

### エージェント構成
- 構成: Opus 直接 + Explore サブエージェント×1（ソースファイル一括読み込み）
- 切り分け: ソース確認は Explore に委任、スキル/ロードマップ作成は Opus 直接

### ツール使用
- プラグイン: なし
- スキル: なし

### 判断の記録
- SKILL.md のフォーマットは baseline-ui を参考に frontmatter + How to use + When to apply + 本体の構成
- ロードマップのタスクグルーピングはプラン段階の案を踏襲（H-A〜H-J の10タスク）
- Agent Hook を採用しない理由: 120秒タイムアウト制限、1ファイル単位では文脈不足、毎 Edit 発火のコストペナルティ

### 発見・ハマり・気づき
- v3roadmap01-2.md の H5（CodeDoctorPage isMounted）は loadProgress が useCallback で定義されており、handleSubmit 内でも呼ばれていないため useCallback 自体を削除可能
- H-C のクライアントタイムスタンプは UI 表示専用のため、solvedAt/completedAt が画面で未使用なら省略が最善

### エラー・障害
- なし

### 影響範囲
- 変更: 4ファイル（新規2、修正2）

### CI結果
- ドキュメントのみの変更のため CI 不要

### 昇華候補メモ
- [RuleOps/tooling.md] `/design-audit` スキルの運用実績を蓄積し、フォーマットの改善を検討

---

## Design Principles 修正（H-A〜H-J）

**ブランチ**: fix/v3-design-principles
**PR**: #204（→ dev マージ済み）
**目的**: v3roadmap02.md の FAIL 18件を修正（Design Principles 7原則準拠）

### 実施内容
- [x] H-A: CodeDoctorPage / MiniProjectDetailPage の useEffect に isMounted ガード追加（P3）
- [x] H-B: MiniProjectDetailPage に loadError 表示追加（P6）
- [x] H-C: solvedAt / completedAt のクライアントタイムスタンプを null に変更（P1）
- [x] H-D: miniProjectService の `as MiniProjectStatus` を型ガード関数に置換（P2）
- [x] H-E: useTokenGenerator のマジックナンバーを定数化（DISTRACTOR_RATIO, MIN_DISTRACTOR_COUNT）（P7）
- [x] H-F: CodeToolbar / Token / TokenPool / AssemblyArea / CodePuzzle に ARIA 属性追加（P4）
- [x] H-G: useTokenGenerator の `as string` / `as T` を undefined ガードに置換（P2）
- [x] H-H: keywordMatcher.ts 抽出 + TestMode / ChallengeMode のインライン判定を共通関数に統一（P5）
- [x] H-I: CodePuzzle に JudgmentResult 接続 + TestMode の CodePuzzle を ErrorBoundary でラップ（P6）
- [x] H-J: ChallengeMode テスト4件 + CodeContext テスト5件を追加（P7）

### エージェント構成
- 構成: Opus 直接（サブエージェント不使用）
- 切り分け: 全タスク [Sonnet] レベルだが、複数タスクが同一ファイルを変更するため直接実行が最も効率的と判断。サブエージェント（code-simplifier）を7並列で試行したが subagent_type 指定誤りで全失敗→直接実行に切替

### ツール使用
- プラグイン: なし
- スキル: なし

### 判断の記録
- H-C: solvedAt（CodeDoctor）/ completedAt（MiniProject）は ProblemCard / ProjectCard の UI で未使用 → null が最もシンプル
- H-A〜H-C / H-F を同一ファイル（CodeDoctorPage / MiniProjectDetailPage）でグルーピングして衝突回避
- H-H で keywordMatcher.ts を features/learning/utils/ に配置（TestMode / ChallengeMode の共通親ディレクトリ）

### 発見・ハマり・気づき
- useCallback+useEffect → inline useEffect+isMounted 変換時、TypeScript strict モードでは nullable 変数の closure 内 narrowing が効かない。async function 内で `user` が null の可能性を排除できない
- 解決策: `const userId = user.id` / `const proj = project` でローカル変数にキャプチャしてから async function を定義

### エラー・障害
- TS18047/TS18048: `user` is possibly null / `project` is possibly undefined（useEffect 内 async closure）→ ローカル変数キャプチャで解決
- `SubmitProjectResult` を import から誤って削除 → grep で検出し再追加
- subagent_type `code-simplifier` → 正しくは `code-simplifier:code-simplifier`。全7件失敗後に直接実行に方針転換

### 影響範囲
- 変更: 14ファイル（新規2、修正12）

### CI結果
- typecheck: PASS / lint: PASS / test: PASS（686件 = 677既存 + 9新規） / build: PASS

### 昇華候補メモ
- [RuleOps/implementation.md] useEffect 内の async closure で nullable 変数を使う場合、early return 後でもローカル変数にキャプチャが必要（TypeScript strict）
- [MEMORY.md] subagent_type は完全修飾名が必要: `code-simplifier:code-simplifier`（短縮名 `code-simplifier` は不可）
